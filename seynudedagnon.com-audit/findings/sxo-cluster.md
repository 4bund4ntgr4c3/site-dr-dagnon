# SXO + Topic Clusters — seynudedagnon.com

Always included in full audits. No DataForSEO / Google API credentials — SERP positions and backlink profile not measured; analysis is structural.

## Search-experience (page-type fit)
- Query intents mapped cleanly: informational "who is / contact / CV" → `/`, `/contact`, `/cv`, `/connect`; research intent → `/publications`, `/bibliography` (DOI, BibTeX/RIS/APA); practitioner intent → `/projets/*` (7 case studies), `/toolkit` (protocols); journalist intent → `/presse` (kit + FAQ), `/inviter`; community intent → `/media/community/*`. No page-type mismatch detected.
- Conversion paths are short: every pillar links to `/contact`, `/inviter`, or `/collaborate`; newsletter capture is sitewide.

## Cluster architecture (hub-and-spoke)
- Hubs: `/projets` (7 spokes), `/tribunes` (3 long-form spokes + canonical external originals), `/publications` (16 papers + 1 blog, explicitly separated), `/media` (5 category hubs → 26 photo leaves).
- User stories: (1) funder validating expertise → home → projets → publications → contact; (2) journalist on deadline → presse → bio/FAQ/photo → inviter; (3) francophone policymaker → /fr/tribunes/G2G → /fr/projets/contrat-g2g → collaborate. All three resolve in ≤3 clicks from home with consistent breadcrumbs.
- Gap: photo leaves link upward but rarely sideways (no "related tribune/projet" on gallery pages) — fixes the thin-content finding and tightens the cluster simultaneously. Add 2–3 contextual links per photo page.
- Gap: `/toolkit` protocols (LLIN, G2G, DHIS2, SMC) are natural pillar pages but have no spoke-level deep dives yet; next content sprint should attach one field-note per protocol rather than minting new top-level sections.
